.worktrees/
node_modules/
dist/
playwright-report/
test-results/

# Host-specific, locally-provisioned browser system libraries (Task 7).
# Never commit downloaded RPM payloads or extracted host binaries.
.superpowers/browser-libs/

# Generated e2e QA screenshots (Task 7). Regenerated by `playwright test`; keep
# them out of clean clones. Committed here because the parent checkout's local
# `.git/info/exclude` (which masks all of `.superpowers/`) is not portable.
.superpowers/sdd/screenshots/
.uxshoot.mjs
.fontfetch.sh
.wsshoot.mjs
.wsshoot2.mjs
